const removeAll = document.querySelector(".btn__limpar__campos");
const itensUl = document.querySelectorAll(".circulo");

function removerTodos() {
    console.log(itensUl);
    removeAll.addEventListener('click', () => {
        //transformar o html colection em array e excluir item por item
    })
}

// removerTodos()
